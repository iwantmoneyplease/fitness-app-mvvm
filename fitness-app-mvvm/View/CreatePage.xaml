<VerticalStackLayout Padding="20">

    <Button 
        Text="Kaffe"
        Command="{Binding CoffeeCommand}" />

    <Button 
        Text="Te"
        Command="{Binding TeaCommand}" />

    <CollectionView 
        ItemsSource="{Binding DrinkOptions}"
        IsVisible="{Binding ShowOptions}">

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Button Text="{Binding Name}" />
            </DataTemplate>

        </CollectionView.ItemTemplate>

    </CollectionView>

</VerticalStackLayout>